# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

.vercel
.nx/*
# Local environment variables
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Local VSCode configuration
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json

# Nx
# =========================================================================
# Nx local cache
.nx/cache
# Nx daemon log, pid and socket
/apps/**/daemon.log
/apps/**/daemon.pid
/apps/**/daemon.sock
/libs/**/daemon.log
/libs/**/daemon.pid
/libs/**/daemon.sock
/tools/**/daemon.log
/tools/**/daemon.pid
/tools/**/daemon.sock
/packages/**/daemon.log
/packages/**/daemon.pid
/packages/**/daemon.sock
# Nx workspace-specific local cache
.nx/뭍* # TODO: Verify this character, it might be a copy-paste error. Should likely be .nx/* or a specific pattern if intended.
         # If it's meant to be .nx/cache or similar, it's covered above.

# Dependencies
# =========================================================================
node_modules/
jspm_packages/
*.tsbuildinfo

# Build outputs
# =========================================================================
dist/
build/
out/
coverage/
public/build/ # If you have a specific build output in public

# Next.js
# =========================================================================
.next/
out/ # Next.js static export output directory

# Testing
# =========================================================================
coverage/
test-output/
junit.xml

# IDEs and editors
# =========================================================================
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
# =========================================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Log files
# =========================================================================
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Temporary files
# =========================================================================
tmp/
temp/
*.tmp
*.bak
*.swp

# Editor directories and files
# =========================================================================
# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*
.vscode

# JetBrains
.idea/*
*.iml

# Playwright
# =========================================================================
test-results/
playwright-report/
playwright/.cache/

# Other
# =========================================================================
# مثلاً ملفات خاصة بالمستخدم أو secrets que no deberían estar en el repo
secrets.json
*.local.xml

# Nx Cloud / Nx Run Cache
# =========================================================================
.nx/cache # Duplicado, ya cubierto arriba

# Snapshots (if generated by a tool and should not be committed)
# =========================================================================
# snapshot-reports/ # Si tus snapshots de código son solo para análisis local y no para el repo
