# Vercel
.vercel/

# Nx
# Ignora todo dentro de .nx (incluye caché, etc.)
.nx/
# Logs/PIDs/Sockets del daemon de Nx si se generan en la raíz de apps/libs
# (Si Nx los guarda dentro de .nx/*, estas líneas son redundantes)
/apps/**/daemon.log
/apps/**/daemon.pid
/apps/**/daemon.sock
/libs/**/daemon.log
/libs/**/daemon.pid
/libs/**/daemon.sock
# /tools/**/daemon.* # Descomentar si usas una carpeta 'tools' para proyectos Nx
# /packages/**/daemon.* # Descomentar si usas una carpeta 'packages' para proyectos Nx

# Local environment variables
.env
.env.*.local
*.env.local # Otra forma de cubrir .env.local, .env.development.local, etc.
# Si quieres ser explícito y tienes muchos, puedes mantenerlos:
# .env.development.local
# .env.test.local
# .env.production.local

# Dependencies
node_modules/
jspm_packages/ # Quitar si no usas jspm

# Build outputs & TypeScript
dist/
build/
out/
coverage/
*.tsbuildinfo
public/build/ # Si generas builds dentro de public

# Next.js
.next/

# Testing
test-output/
junit.xml # Si generas reportes JUnit

# OS generated files
.DS_Store
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log* # Quitar si no usas Lerna

# Temporary files
tmp/
temp/
*.tmp
*.bak
*.swp # Cubre .swa, .swb, etc.

# IDEs & Editors
# Visual Studio Code
# Ignora todo en .vscode por defecto...
.vscode/*
# ...excepto los archivos que quieres compartir
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json
!.vscode/tasks.json
# Extensión VSCode "Local History"
.history/

# JetBrains (IntelliJ, WebStorm, etc.)
.idea/
*.iml

# Visual Studio
*.suo
*.ntvs*
*.njsproj
*.sln

# Playwright
test-results/
playwright-report/
playwright/.cache/

# User-specific files / Secrets (Ejemplos)
secrets.json
*.local.xml

# Snapshots (generados por tu herramienta)
# Descomentar si no quieres que estén en el repositorio
# snapshot-reports/
